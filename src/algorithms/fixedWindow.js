class FixedWindow {
    constructor(options, storage) {
        this.limit = options.limit;
        this.window = options.window;
        this.store = store
    }


    async check (identifier){
        
    }
}